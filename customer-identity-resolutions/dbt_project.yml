name: kanoniv_cloud
version: '1.0.0'
profile: kanoniv_cloud

model-paths: ["models"]
macro-paths: ["macros"]
seed-paths: ["data"]

seeds:
  kanoniv_cloud:
    +schema: raw

clean-targets:
  - target
  - dbt_packages

models:
  kanoniv_cloud:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: resolved
